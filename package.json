{
  "name": "currency-ex",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server": "nodemon server.js",
    "client": "cd client && npm start",
    "start": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
    "build": "cd client && npm && npm run build",
    "install": "cd client && npm install && npm run build && cd .."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "concurrently": "^6.0.2",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "validator": "^13.5.2"
  },
  "engines": {
    "node": "12.16.1",
    "npm": "6.14.8"
  }
}
